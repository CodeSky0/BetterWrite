# 统一行尾为 LF，与 Biome 格式化器（默认 LF）及 CI（ubuntu）保持一致。
# 避免 Windows 下 core.autocrlf=true 检出 CRLF 导致 `pnpm lint` 全仓库报行尾错误。
* text=auto eol=lf

# 明确的二进制类型，禁止行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
